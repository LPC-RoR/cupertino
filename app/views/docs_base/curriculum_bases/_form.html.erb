<%= form_with(model: curriculum_base, local: true) do |form| %>
  <% if curriculum_base.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curriculum_base.errors.count, "error") %> prohibited this curriculum_base from being saved:</h2>

      <ul>
      <% curriculum_base.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orden %>
    <%= form.number_field :orden %>
  </div>

  <div class="field">
    <%= form.label :curriculum_base %>
    <%= form.text_field :curriculum_base %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
