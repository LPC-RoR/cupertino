<p id="notice"><%= notice %></p>

<h1>Referencias</h1>

<table>
  <thead>
    <tr>
      <th>Nivel base</th>
      <th>Asignatura base</th>
      <th>Documento base</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @referencias.each do |referencia| %>
      <tr>
        <td><%= referencia.nivel_base_id %></td>
        <td><%= referencia.asignatura_base_id %></td>
        <td><%= referencia.documento_base_id %></td>
        <td><%= link_to 'Show', referencia %></td>
        <td><%= link_to 'Edit', edit_referencia_path(referencia) %></td>
        <td><%= link_to 'Destroy', referencia, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Referencia', new_referencia_path %>
