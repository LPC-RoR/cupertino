<%= form_with(model: tipo_referencia_base, local: true) do |form| %>
  <% if tipo_referencia_base.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_referencia_base.errors.count, "error") %> prohibited this tipo_referencia_base from being saved:</h2>

      <ul>
      <% tipo_referencia_base.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orden %>
    <%= form.number_field :orden %>
  </div>

  <div class="field">
    <%= form.label :tipo_referencia_base %>
    <%= form.text_field :tipo_referencia_base %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
